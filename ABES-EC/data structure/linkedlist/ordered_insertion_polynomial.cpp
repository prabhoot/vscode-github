#include "phll.h"
#include <cstddef>
void oipll(struct node **start,int c,int e){
}
int main(){
    struct node *poly=NULL;
    insbeg(&poly,3,5);
    insbeg(&poly,4,4);
    insbeg(&poly,5,3);
    insbeg(&poly,6,2);
    traverse(&poly);
    return 0;
}
int main() { return 0; }